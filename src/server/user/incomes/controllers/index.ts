import referralIncome from "./referralIncome";
import roi from "./roi";
import getReferralIncomeSummary from "./getReferralIncomeSummary";

const incomeController = {
  getReferralIncomeSummary,
  referralIncome,
  roi,
};
export default incomeController;
